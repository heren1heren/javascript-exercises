const getTheTitles = function(books) {
   //use map() built-in function for the new array instead of push and for..of loop

      return   title = books.map(obj => obj.title);
      
};

// Do not edit below this line
module.exports = getTheTitles;
